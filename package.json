{
  "name": "techspec",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "format": "npx @biomejs/biome format --write backend frontend",
    "format:check": "npx @biomejs/biome format backend frontend",
    "lint": "npx @biomejs/biome lint --write backend frontend",
    "lint:check": "npx @biomejs/biome lint backend frontend",
    "check": "npx @biomejs/biome check --write backend frontend",
    "check:ci": "npx @biomejs/biome check backend frontend",
    "knip": "knip",
    "knip:fix": "knip --fix"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.5",
    "knip": "^5.69.1"
  },
  "dependencies": {
    "@fontsource/open-sans": "^5.2.7"
  }
}
